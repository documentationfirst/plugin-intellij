# permanent-context-architecture

Voir `documents/specification/permanent-context-architecture.md` pour le contenu complet.
Ce fichier est la copie migrée vers `tasks/specification/` (structure v2).

La structure de référence `.ai_context/` utilise `tasks/` au lieu de `documents/`.
Tous les comportements (permanent-*, reset au changement de contexte) sont identiques.

